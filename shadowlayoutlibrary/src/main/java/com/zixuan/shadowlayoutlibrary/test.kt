package com.zixuan.shadowlayoutlibrary

import android.graphics.Path

private var mPath = Path()
var a : Int = 0
class test constructor() {
    private fun drawChild(canvas: Int, block: (Int) -> Unit) {

    }

    private fun get(){
        drawChild(1){}
    }
}